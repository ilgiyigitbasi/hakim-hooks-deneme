const state = {
  // User Details için state
};

const getters = {
  // User Details için getters
};

const mutations = {
  // User Details için mutations
};

const actions = {
  // User Details için actions
};

export default {
  state,
  getters,
  mutations,
  actions,
};
