const state = {
  // Dashboard için state
};

const getters = {
  // Dashboard için getters
};

const mutations = {
  // Dashboard için mutations
};

const actions = {
  // Dashboard için actions
};

export default {
  state,
  getters,
  mutations,
  actions,
};
