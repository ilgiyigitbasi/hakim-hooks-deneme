const state = {
  // Report detail için state
};

const getters = {
  // Report detail için getters
};

const mutations = {
  // Report detail için mutations
};

const actions = {
  // Report detail için actions
};

export default {
  state,
  getters,
  mutations,
  actions,
};
